/*
 * ============================================================
 *  AUTO-UPDATE — optional. Fill in your GitHub repo to enable.
 * ============================================================
 *  When configured, the installed app checks your GitHub Releases
 *  on startup and, if a newer version is published, downloads it
 *  and installs it on quit — so your friend always has the latest
 *  version without you re-sending the .exe.
 *
 *  Setup (see README "Auto-update setup"):
 *    1. Create a free public GitHub repo (e.g. "club-sandwich-streaming").
 *    2. Put OWNER and REPO below.
 *    3. Add matching "publish" config to package.json (README shows it).
 *    4. Bump "version" in package.json, then `npm run release`.
 *
 *  Leave OWNER/REPO blank to disable auto-update entirely (default).
 * ============================================================
 */

module.exports = {
  OWNER: "Official-Nugget",
  REPO: "q8k3n7x2",
};
